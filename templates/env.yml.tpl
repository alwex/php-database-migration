connection:
    host:     {HOST}
    driver:   {DRIVER}
    port:     {PORT}
    username: {USERNAME}
    password: {PASSWORD}
    database: {DATABASE}
    charset:  {CHARSET}

changelog: {CHANGELOG}
default_editor: {EDITOR}

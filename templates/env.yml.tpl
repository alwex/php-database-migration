connection:
    host:       {HOST}
    driver:     {DRIVER}
    port:       {PORT}
    username:   {USERNAME}
    password:   {PASSWORD}
    database:   {DATABASE}
    charset:    {CHARSET}
    ssl-ca-cert: {SSLCACERT}
    ssl-cert:   {SSLCERT}
    ssl-key:    {SSLKEY}

changelog: {CHANGELOG}
default_editor: {EDITOR}
